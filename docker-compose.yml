version: '3.8'

services:
  notification-service:
    build: .
    image: notification-service:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
